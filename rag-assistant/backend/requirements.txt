# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.29.0
python-multipart==0.0.9

# Google Gemini LLM
google-generativeai==0.7.2

# Google ADK (agent layer)
google-adk==0.1.0

# Embeddings + reranking (local, no API key)
sentence-transformers==3.0.1
faiss-cpu>=1.8.0

# PyTorch CPU-only (smaller image, sufficient for inference)
torch==2.3.1+cpu
torchvision==0.18.1+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Document parsing
PyMuPDF==1.24.5
markdown==3.6

# Async SQLite
sqlalchemy==2.0.30
aiosqlite==0.20.0

# Config + utilities
pydantic>=2.7.3
pydantic-settings>=2.5.2
python-dotenv==1.0.1
numpy==1.26.4          # must stay <2 for torch 2.3.1 compatibility
transformers==4.41.2
tokenizers==0.19.1     # litellm upgrades this to 0.22 which breaks transformers 4.41
